
// Shows the control box under the element that has clicked_id as id
function showControlbox(clicked_id) {
    let clicked = document.getElementById(clicked_id)
    let controlbox = document.createElement("div")
    // Those attributes are stub value, thus should be changed in near future
    controlbox.style.backgroundColor = "beige"
    controlbox.style.width = "16px"
    controlbox.style.height = "16px"
}